{% extends 'base.html.twig' %}

{% block title %}Seeker{% endblock %}

{% block body %}
    <h1>Seeker</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ seeker.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ seeker.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ seeker.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('seeker_index') }}">back to list</a>

    <a href="{{ path('seeker_edit', {'id': seeker.id}) }}">edit</a>

    {{ include('seeker/_delete_form.html.twig') }}
{% endblock %}
