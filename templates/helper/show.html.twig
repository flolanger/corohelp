{% extends 'base.html.twig' %}

{% block body %}
    <a class="uk-margin-top" href="{{ app.request.headers.get('referer') }}" uk-icon="icon: arrow-left; ratio: 2.5"></a>
    <article class="uk-margin-top uk-article">
        <h1 class="uk-article-title"><a class="uk-link-reset" href="">{{ helper.title }}</a></h1>
        <p class="uk-article-meta">{% trans %}posts.writtenBy{% endtrans %} <a href="#">{{ helper.user.name }}</a>
            {% trans %}posts.on{% endtrans %} {{ helper.creationDate|date("d.m.Y") }}</p>
        <p>{{ helper.description }}</p>
        <div class="uk-section">
            <div class="uk-container">
                <h3>{% trans %}posts.contact{% endtrans %}</h3>
                <a href="mailto:{{ helper.user.email }}" uk-icon="icon: mail"></a><a href="mailto:{{ helper.user.email }}" class="uk-margin-small-left">{{ helper.user.email }}</a>
                {% if helper.user.phoneNumber %}
                    <div class="uk-margin-small-top"></div>
                    <a href="tel:{{ helper.user.phoneNumber }}" uk-icon="icon: receiver"></a>
                    <a href="tel:{{ helper.user.phoneNumber }}" class="uk-margin-small-left">{{ helper.user.phoneNumber }}</a>
                {% endif %}
            </div>
            <div>
                <div class="uk-float-right">
                    {{ include('helper/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </article>
{% endblock %}
