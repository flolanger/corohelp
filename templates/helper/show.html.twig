{% extends 'base.html.twig' %}

{% block title %}Helper{% endblock %}

{% block body %}
    <h1>Helper</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ helper.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ helper.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ helper.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('helper_index') }}">back to list</a>

    <a href="{{ path('helper_edit', {'id': helper.id}) }}">edit</a>

    {{ include('helper/_delete_form.html.twig') }}
{% endblock %}
